[project]
name = "straw-runtime-integration"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = ["straw-sdk @ git+https://github.com/beremaran/straw-sdk-python.git@v0.2.0"]

[tool.uv]
package = false
