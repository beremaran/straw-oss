[project]
name = "straw-sdk"
version = "0.1.0"
description = "Minimal Python client for Straw's public REST request transport."
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["straw*"]
