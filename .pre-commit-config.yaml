minimum_pre_commit_version: "3.0.0"
default_stages: [pre-commit]
repos:
  - repo: local
    hooks:
      - id: make-check
        name: make check
        entry: make check
        language: system
        pass_filenames: false
