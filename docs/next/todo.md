# TODO

- in-memory cert cache
- standard forwarding proxy mode (tunneling mode) - just for ip masking, geo-unblocking, simple allow/deny domain lists etc similar to squid
  - flag controls tunneling mode vs mitm inspection/rewriting/editing/ad-hoc interception mode similar to `mitmproxy`
